# Compila la app de iOS en un Mac de GitHub. En repos publicos los runners
# macOS son gratis, asi que esto sustituye a tener un Mac para el build.
#
# Funciona en dos modos, segun haya secretos configurados o no:
#
#   SIN cuenta Apple  -> compila sin firmar. No se puede instalar ni publicar,
#                        pero responde a la unica pregunta que importa antes de
#                        pagar los 99 EUR al ano: compila esto?
#   CON cuenta Apple  -> firma y exporta el .ipa como artefacto descargable.
#
# Secretos para el modo firmado (Settings > Secrets and variables > Actions):
#   IOS_CERTIFICATE_P12        certificado de distribucion en base64
#   IOS_CERTIFICATE_PASSWORD   contrasena con la que se exporto el .p12
#   IOS_PROVISION_PROFILE      perfil de aprovisionamiento en base64
#   IOS_TEAM_ID                identificador del equipo (10 caracteres)
#
# Para pasar un fichero a base64:  base64 -i certificado.p12 | pbcopy
#
# Nota: el contexto `secrets` NO existe en el `if` de un step, solo en `env`.
# Por eso el job define TIENE_CERT una vez y los pasos miran esa variable.

name: iOS

on:
  workflow_dispatch:          # se lanza a mano desde la pestana Actions
  push:
    tags: ['v*']              # y en cada etiqueta de version

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 40
    env:
      TIENE_CERT: ${{ secrets.IOS_CERTIFICATE_P12 != '' }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Instalar dependencias
        run: npm ci

      - name: Preparar www y sincronizar iOS
        run: |
          npm run build
          npx cap sync ios

      # ---------- modo firmado: solo si hay certificado ----------
      - name: Importar certificado y perfil
        if: env.TIENE_CERT == 'true'
        env:
          CERT: ${{ secrets.IOS_CERTIFICATE_P12 }}
          CERT_PASS: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
          PERFIL: ${{ secrets.IOS_PROVISION_PROFILE }}
        run: |
          set -euo pipefail
          # llavero temporal: vive solo lo que dura el job
          CLAVE_LLAVERO=$(uuidgen)
          security create-keychain -p "$CLAVE_LLAVERO" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$CLAVE_LLAVERO" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain

          echo "$CERT" | base64 --decode > /tmp/cert.p12
          security import /tmp/cert.p12 -k build.keychain -P "$CERT_PASS" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$CLAVE_LLAVERO" build.keychain

          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "$PERFIL" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/perfil.mobileprovision
          rm -f /tmp/cert.p12

      - name: Archivar y exportar .ipa
        if: env.TIENE_CERT == 'true'
        env:
          TEAM_ID: ${{ secrets.IOS_TEAM_ID }}
        run: |
          set -euo pipefail
          cat > /tmp/ExportOptions.plist <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key><string>app-store-connect</string>
            <key>teamID</key><string>${TEAM_ID}</string>
            <key>uploadSymbols</key><true/>
            <key>signingStyle</key><string>automatic</string>
          </dict>
          </plist>
          PLIST

          xcodebuild -project ios/App/App.xcodeproj \
            -scheme App \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath /tmp/App.xcarchive \
            DEVELOPMENT_TEAM="$TEAM_ID" \
            archive

          xcodebuild -exportArchive \
            -archivePath /tmp/App.xcarchive \
            -exportOptionsPlist /tmp/ExportOptions.plist \
            -exportPath /tmp/salida

      # ---------- modo sin firmar: cuando aun no hay cuenta Apple ----------
      - name: Compilar sin firmar
        if: env.TIENE_CERT != 'true'
        run: |
          set -euo pipefail
          echo "Sin certificado: se compila sin firmar solo para comprobar que el proyecto es valido."
          xcodebuild -project ios/App/App.xcodeproj \
            -scheme App \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath /tmp/App.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            archive

      - name: Guardar el .ipa
        if: env.TIENE_CERT == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: back2prime-ios
          path: /tmp/salida/*.ipa
          if-no-files-found: error
          retention-days: 30

      - name: Limpiar el llavero
        if: always()
        run: security delete-keychain build.keychain || true
